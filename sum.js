//Can you write a simple code. This piece of code must count the number of digit
//count 12=2

function countDigits(num) {
    num = num.toString().length;
    2;
    console.log(num);
}
let x = 15;
countDigits(x);

